import { Meta, Story, ArgsTable, Canvas } from "@storybook/addon-docs/blocks"
import { Contact } from "ui"
import FooterBlock from "."

export const footerInfo = {
  branding: {
    to: "/",
    width: 100,
    ariaLabel: "Back to home",
  },
}

export const contactInfo = [
  {
    title: "Email",
    icon: "comment",
    text: "hq@octahedroid.com",
  },
  {
    title: "Schedule",
    icon: "calendar",
    text: `<a
              href="https://calendly.com/jmolivas/free-one-to-one-consultation"
              target="_blank"
              rel="noreferrer"
              }}
            >
              Free consultation
            </a>`,
  },
  {
    title: "Phone",
    icon: "phone",
    text: "760-618-1215",
  },
]

<Meta title='blocks/FooterBlock' component={FooterBlock} />

# FooterBlock

You can use it to add a Footer block

## Props

<ArgsTable of={FooterBlock} />

export const FooterBlockTemplate = (args) => <FooterBlock {...args} />

### Default FooterBlock

<Canvas>
  <Story
    name='Default'
    args={{
      ...footerInfo,
    }}
  >
    {FooterBlockTemplate.bind({})}
  </Story>
</Canvas>

### Full Width

<Canvas>
  <Story
    name='Full Width'
    args={{
      ...footerInfo,
      isFull: true,
    }}
  >
    {FooterBlockTemplate.bind({})}
  </Story>
</Canvas>

### Light variant

<Canvas>
  <Story
    name='Light variant'
    args={{
      ...footerInfo,
      variant: "light",
    }}
  >
    {FooterBlockTemplate.bind({})}
  </Story>
</Canvas>

### With Contack Block

<Canvas>
  <Story
    name='With Contact Block'
    args={{
      ...footerInfo,
      variant: "light",
    }}
  >
    <FooterBlock isFull variant='light' align='left' branding={footerInfo.branding}>
      <Contact>
        <Contact.Header>
          Tell us about your project. And we will contact you promptly to discuss
          next steps.
        </Contact.Header>
        <Contact.Grid contactInfo={contactInfo} />
      </Contact>
    </FooterBlock>
  </Story>
</Canvas>
