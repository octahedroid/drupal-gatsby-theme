import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks"
import { Box } from "theme-ui"
import Logo from "."

export const BLogo = (args) => {
  return <Logo {...args} />
}

<Meta
  title='components/Logo'
  component={Logo}
  argTypes={{ fill: { control: "color" } }}
/>

# Logo

Gatsby logo

## Props

<Props of={Logo} />

### Default

<Preview>
  <Story name='Default'>{BLogo.bind({})}</Story>
</Preview>

### Light version

<Preview>
  <Story name='Light version'>
    <Box sx={{ bg: "darkShade" }}>
      <Logo width={200} fill='#fff' />
    </Box>
  </Story>
</Preview>
