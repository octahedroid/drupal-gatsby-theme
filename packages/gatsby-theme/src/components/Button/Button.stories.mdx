import { Meta, Story, ArgsTable, Canvas } from "@storybook/addon-docs/blocks"
import Button from "."

<Meta title='components/Button' component={Button} />

# Button

You can use it as a button or a link, this component accepts a variant

## Props

<ArgsTable of={Button} />

### Default button

<Canvas>
  <Story name='Default'>
    <Button to='/'>Default button</Button>
  </Story>
  <Story name='primary'>
    <Button to='/' variant='primary'>
      Primary button
    </Button>
  </Story>
</Canvas>

### Secondary button

<Canvas>
  <Story name='Secondary'>
    <Button to='/' variant='secondary'>
      Secondary button
    </Button>
  </Story>
</Canvas>

### Light button

<Canvas>
  <Story name='Light'>
    <Button to='/' variant='light'>
      Secondary button
    </Button>
  </Story>
</Canvas>

### Dark button

<Canvas>
  <Story name='Dark'>
    <Button to='/' variant='dark'>
      Secondary button
    </Button>
  </Story>
</Canvas>

### Link

<Canvas>
  <Story name='Link'>
    <Button as='a' to='/'>
      I&apos;m a link
    </Button>
  </Story>
</Canvas>

### Transparent

<Canvas>
  <Story name='Transparent'>
    <Button to='/' variant='transparent'>
      Transparent button
    </Button>
  </Story>
</Canvas>
