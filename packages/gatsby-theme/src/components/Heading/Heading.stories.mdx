import { Meta, Story, ArgsTable, Canvas } from "@storybook/addon-docs/blocks"
import Heading from "."

export const headingArgTypes = {
  as: {
    description: "HTML Tag",
    control: {
      type: "select",
      options: ["h1", "h2", "h3", "h4", "h5", "h6"],
    },
  },
  variant: {
    description: "Style variants",
    control: {
      type: "select",
      options: ["default", "big", "intro"],
    },
  },
}

<Meta title='components/Heading' component={Heading} />

# Heading

Common headings h1 to h6, default is h2. This component accepts a variant.

## Props

<ArgsTable of={Heading} />

export const HeadingTemplate = (args) => <Heading {...args} />

### Default

<Canvas>
  <Story
    args={{ children: "Default is h2, change the tag in the controls tab." }}
    argTypes={headingArgTypes}
    name='Default'
  >
    {HeadingTemplate.bind({})}
  </Story>
</Canvas>

### Big heading

<Canvas>
  <Story
    name='Big heading'
    args={{ children: "Big variant", variant: "big" }}
    argTypes={headingArgTypes}
  >
    {HeadingTemplate.bind({})}
  </Story>
</Canvas>

### Intro heading

<Canvas>
  <Story
    name='Intro heading'
    args={{ children: "Intro variant", variant: "intro" }}
    argTypes={headingArgTypes}
  >
    {HeadingTemplate.bind({})}
  </Story>
</Canvas>

### With ID

<Canvas>
  <Story
    name='With ID'
    args={{ children: "A nice title", id: "anchor" }}
    argTypes={headingArgTypes}
  >
    {HeadingTemplate.bind({})}
  </Story>
</Canvas>

### With ID no anchor

<Canvas>
  <Story
    name='With ID no anchor'
    args={{ children: "A nice title", id: "anchor", anchor: false }}
    argTypes={headingArgTypes}
  >
    {HeadingTemplate.bind({})}
  </Story>
</Canvas>

### All sizes

<Canvas>
  <Story name='All sizes'>
    <Heading as='h1'>H1</Heading>
    <Heading as='h2'>H2</Heading>
    <Heading as='h3'>H3</Heading>
    <Heading as='h4'>H4</Heading>
    <Heading as='h5'>H5</Heading>
    <Heading as='h6'>H6</Heading>
  </Story>
</Canvas>
